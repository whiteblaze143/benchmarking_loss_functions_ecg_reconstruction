CRITICAL: none.

MAJOR:

1. **Current scientific release gate is failed, but deployment tooling does not enforce it.** `book/_book/13_engineering_ml_visual_atlas.html:950-992` reports `final completion = FAIL`, while `scripts/build_book_release_manifest.py:19-22` checks only mechanical audit fields and still assigns a verified release label at line 38. Minimum fix: require every mandatory scientific gate, including `FINAL_COMPLETION.json`, to pass before production manifest creation/deployment; otherwise emit an explicitly provisional manifest and block promotion.

2. **Public HTML exposes patient/record-level identifiers linked to rhythm labels.** `book/16_three_lead_ecgaim_live.qmd:539-549` joins and publishes `record_id`, `patient_id`, and `released_rhythm`; the rendered Plotly payload at `book/_book/16_three_lead_ecgaim_live.html:4499` contains 360 distinct `MUSE_...` acquisition identifiers plus record IDs and rhythm categories. Minimum fix: replace public identifiers with release-specific random IDs, omit acquisition filenames/patient IDs from HTML, keep the mapping private, and add a release-time PHI/identifier scan.

3. **“Live” results are neither transactionally coherent nor hash-bound.** Chapters 15–17 perform separate reads while writers remain active (`book/15_live_results_observatory.qmd:20-54`), and the helper opens a new SQLite transaction for each query. The current one-lead queue has a 4 KiB base DB with an approximately 1.39 MiB newer WAL, yet rendered Chapters 15/17 report base size `0.00 MB` and the previous-day base mtime while reading current WAL rows. `scripts/build_book_release_manifest.py:28-34` omits most live databases and hashes raw SQLite base files without WAL-consistent snapshots. Minimum fix: create read-only SQLite backup snapshots at build start, render every chapter from those immutable snapshots, and hash every snapshot with a shared snapshot/generation ID.

4. **Atomic deployment is not implemented.** Chapter 13 itself reports this as pending (`book/13_engineering_ml_visual_atlas.qmd:182`), while `scripts/render_quarto_chapters.py:63` renders each chapter directly into live `book/_book`; an interruption leaves a mixed-generation site. The manifest is also written directly at `scripts/build_book_release_manifest.py:42`. Minimum fix: render and audit a complete versioned staging directory, write its manifest atomically, then switch a symlink/version pointer or rename the staged artifact only after every gate passes.

5. **The deployable artifact still depends on remote runtime JavaScript despite the resource-closure claim.** `_quarto.yml:64-67` disables embedding, and rendered pages load CDN JQuery, RequireJS, Plotly, MathJax, and Polyfill; for example `book/_book/16_three_lead_ecgaim_live.html:67,118,976-977`. `book/README.md:22-27` says dependencies are contained under `site_libs`, but `scripts/audit_quarto_book.py:33-34,121,125-127` merely inventories external URLs and does not fail on them. Minimum fix: vendor pinned runtime assets, distinguish runtime dependencies from ordinary hyperlinks, fail on unapproved remote scripts/styles, and hash every deployed resource.

6. **Render resumption and manifest creation are not bound to their inputs.** `scripts/render_quarto_chapters.py:56` skips a chapter solely because an old state says `complete`, without checking QMD, `_quarto.yml`, helper-code, environment, or database hashes. `scripts/build_book_release_manifest.py:18-27` trusts caller-supplied old audit/render-state files and hashes current QMD/HTML without proving they belong to that run. The fresh audit currently reports six wrong-renderer pages and one stale page; Chapter 14 is still Quarto 1.4.555. Minimum fix: bind each state/audit to a run ID and hashes of all sources, helpers, configuration, environment lock, and data snapshots; rerun the audit inside manifest creation. Also add `stale_html` to the auditor’s exit condition—currently omitted at `audit_quarto_book.py:125-127`.

7. **The timeout path crashes instead of recording a controlled render failure.** `scripts/render_quarto_chapters.py:64-65` concatenates `TimeoutExpired.stdout` with strings; captured timeout output is bytes even with `text=True`, producing `TypeError: can't concat str to bytes`. It also kills only the Quarto parent, potentially leaving a Jupyter kernel running. Minimum fix: decode timeout output defensively, launch each render in a process group, terminate the full group on timeout, and persist an error state in `finally`.

8. **The Python environment is machine-specific and not reproducible.** `_quarto.yml:80` relies on a user-registered `venv-kernel`; `render_quarto_chapters.py:50-53`, Chapter 13, Chapter 15, and `book/README.md:13-20` hard-code `/home/mithunmanivannan/.venv`, while the manifest records neither Python/package versions nor a lockfile digest. Minimum fix: provide a checked-in locked environment and deterministic kernel setup, use repository-relative configuration, and record Python, platform, and dependency-lock hashes.

9. **Generation identity is inferred from model-name strings rather than verified artifacts.** `book/16_three_lead_ecgaim_live.qmd:76-92` labels every `f_` row “canonical” without joining a generation manifest or displaying `evaluation_version`; the current DB mixes `legacy_v1` and `missing_leads_v2`. Lines 663-668 merge blinded and oracle protocols only on `model_id`, without asserting checkpoint/dataset digests. Minimum fix: join all displayed rows to a locked manifest and require checkpoint, evaluator, protocol, preprocessing, dataset, and generation hashes to match before comparison.

10. **Multiplicity is ignored in the main clinical paired-inference ranking.** `book/16_three_lead_ecgaim_live.qmd:615-620` flags confidence intervals excluding zero, sorts 1,576 current comparisons by raw `p_value`, and shows the top 100, although Chapter 14 requires multiplicity control. Minimum fix: define confirmatory families/endpoints, show adjusted p-values or simultaneous intervals, and label all unadjusted screens exploratory.

11. **Representation-study confidence intervals assume record independence without verified person identity.** `book/16_three_lead_ecgaim_live.qmd:341-343` reports 2,000 record-level bootstrap CIs while explicitly admitting cached one-row-per-ID data do not prove distinct source patients. Minimum fix: recover a trustworthy person mapping, split and bootstrap by person/site, or remove inferential CI/p-value language and label the estimates record-level descriptive.

12. **Several showcased plots select an arbitrary database row rather than a prespecified checkpoint.** `book/16_three_lead_ecgaim_live.qmd:486-490` uses `cm.model_id.iloc[-1]`, and lines 539-549 similarly use `projection.model_id.iloc[-1]`; neither SQL query has deterministic ordering. Minimum fix: expose an explicit locked model/checkpoint control, filter by exact job/checkpoint digest, and assert uniqueness/completeness.

13. **Chapter 17 advertises mechanism tests but does not compute the registered mechanism contrasts.** The hierarchy specifies sequential wavelet/SSL and Morlet-phase-versus-UEG-phase contrasts at `book/17_one_lead_wavelet_ssl_live.qmd:40-54`, but lines 60-65 compare every configuration only with `A0_raw`; lines 91-95 call single-run values “mean” while the current replication audit has fewer than two seeds throughout. Minimum fix: implement exact matched incremental and R0-versus-R1 contrasts across seed, lead, optimizer, architecture, and compute, and keep the page explicitly screening-only until replication exists.
