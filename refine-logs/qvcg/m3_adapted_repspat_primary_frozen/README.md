# Frozen M3 Primary

`M3_ADAPTED_REPSPAT_PRIMARY` is complete and checksum-frozen.

- Pairwise structure: nontrivial (953 BH non-rejection edges; 1,063 BH rejections).
- Connected-component result: one component containing all 64 domains.
- Clique status: false (`AMBIGUOUS_NON_CLIQUE`).
- Categorical quotient gate: **FAIL**.

The component is not a valid motif or equivalence class. The frozen
`VCG_VOXEL_TO_MOTIF.parquet` records the historical connected-component output
but must not be used as a qualified categorical tokenizer. The authoritative
interpretation is in `../QVCG_CC_MOTIF_GATE.json`.

