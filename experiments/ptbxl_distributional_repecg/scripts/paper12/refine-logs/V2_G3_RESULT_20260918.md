# Paper 12 V2 G3 Result

**Verdict: PASS.** This does not alter the immutable V1 failure. V2 uses fold-1–6 phase-coordinate standardization with floor `1e-6`; no coordinate required flooring.

Across phase origins 0, 4, 8 and 12, conditional NLL was `-0.288` to `-0.208` versus phase-only NLL `0.486` to `0.488`. Patient-bootstrap conditional-minus-phase-only intervals were wholly negative at every origin.

Wrong-patient NLL was `1.436` to `1.797`; label-matched wrong-history NLL was `5.350` to `6.373`. Every correct-minus-wrong patient-bootstrap interval excluded zero in the required direction.

Innovation location dependence decreased by `0.255` to `0.307`, and innovation scale dependence decreased versus mean residuals by `0.177` to `0.214`. Every patient-bootstrap interval excluded zero at every origin.

Ordered origin-0 NLL was `-0.2877` versus `0.1490` after deterministic record-specific phase permutation. The paired ordered-minus-permuted difference was `-0.4367`, 95% CI `[-0.4439,-0.4293]`, over 1,871 patients.

Innovation mean was `0.026` to `0.035` and variance `1.019` to `1.036`. Clamp occupancy was effectively zero. These calibration quantities are descriptive, consistent with the preregistered protocol.

V2 therefore establishes record-specific ordered conditional location and scale structure beyond train-fitted phase marginals in the frozen standardized Nyström coordinate basis. It does not establish basis invariance, Gaussianity, covariance whitening or causal independence. G4 may test diagnostic retention with matched frozen probes.

## Donor-control correction

A post-run implementation audit found that the original deterministic donor rule concentrated 2,176 targets onto 2 unrestricted and 38 label-matched donors. That wrong-history evidence was invalidated. The rule was replaced by a deterministic one-to-one assignment: 2,176 unique donors, maximum reuse one, every donor a different patient, and 99.91% exact label-vector preservation in the G3-only matched control. The complete frozen V2-G3 run was repeated. Corrected wrong-patient NLL was `5.085–6.543` and corrected label-matched NLL was `3.955–4.991`; all patient-bootstrap correct-minus-wrong intervals remained wholly negative at every origin. Other deterministic results reproduced exactly. The corrected rerun is the authoritative G3 artifact.
