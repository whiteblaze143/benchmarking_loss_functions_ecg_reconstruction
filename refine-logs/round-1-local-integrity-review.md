# Round 1 Local Integrity Review

**Review type:** same-agent, evidence-forensic review; **not independent and not cross-family**.

**Verdict:** REVISE / IN PROGRESS

## Problem-anchor fidelity

The endpoint-aware factorial benchmark remains aligned with the frozen anchor. The work continues to ask which loss components preserve missing-lead ECG information, rather than assuming that the largest composite objective wins. No architecture novelty is being forced into the contribution.

## Blocking findings resolved this round

1. The older queue count combined incompatible historical checkpoints with the current source/batch contract. Incompatible and unrecoverable completions were quarantined and requeued.
2. A mask-only temporal-MMD CSV could alias old and retrained models. It is now excluded from current claims, and the replacement keys output by model ID, seed, checkpoint SHA, source SHA, test-data root, target-feature-cache SHA, and evaluator SHA.
3. Several book chapters presented random values as empirical engineering or clinical outcomes. Those sections were withdrawn and replaced with measured operational diagnostics or explicit validation protocols.
4. The smartwatch chapter used the wrong observed lead, wrong downstream classifier name, non-matching hard-coded metrics, and unsupported hardware-causal explanations. It now loads the 192-row locked result table and the true Lead-II/ECGFounder/simulator protocol.
5. EchoNext depended on a loose JSON listed in the manifest but absent locally. Reference and fidelity statistics are now recomputed from 13 locked record-level Parquet members in the archived bundle.
6. The checkpoint archiver redundantly treated materialized inference caches as new local training checkpoints. Cached copies are now excluded from archive selection; the combined checkpoint-store, target-cache, and artifact-gate suite now passes 23 focused tests.
7. The architecture, preprocessing, and loss chapters described generic or invented implementations. They now read the actual registry, tensor/training contract, and `common_loss.py`: I/II/V2 replaces the erroneous II/V1/V5 mapping; MCMA-U-Net/MultiScale-VAE/ECG-AIM replace unevaluated ResNet/Conformer claims; and energy distance, the Kors transform, coefficients, and five-level MMD selector match executable source.
8. A stale inference report reused a current-looking model filename with an obsolete checkpoint/source digest. It was regenerated from the approved archived generation, and the book now refuses to display a report unless checkpoint, source, contract, test-content root, output existence, and finiteness all pass.
9. The first strict temporal-morphology artifact for `f_1000000_s42` passed its digest, denominator, batch-failure, pairing, and accounting gates. A subsequent metric-content review correctly removed it from acceptance when the evaluator name was found to imply MMD without emitting an explicit distribution-MMD value.
10. The Zhejiang chapter was previously outside the book and claimed an MSE-versus-MMD result from a one-row ground-truth ceiling file. It is now a connected Part-I actual-data audit and explicitly reports that the contrast is not estimable.
11. Exact inference was demonstrated across every compatible archived identity on one real PTB-XL record. The cohort-aware watcher reruns only when its compatibility-audit hash changes, binds code/input/audit/outputs by SHA-256, records compact missing-lead diagnostics, and retains zero checkpoint-cache bytes.
12. Order-truncated temporal feature pairing was replaced with monotonic maximum-cardinality/minimum-time one-to-one matching within 100 ms. Accepted rows must account exactly for paired and unmatched target/reconstructed events and bind finite median/p95 timing error.
13. Adding detector sample indices exposed 6,707 duplicate QT rows caused by reuse of one QRS onset for multiple later T offsets, including multi-second pseudo-QT values. QT extraction now permits at most one T offset within each detected QRS-to-next-QRS cycle, and both helper/caller source enter the extractor digest.
14. The temporal evaluator now emits an actual biased squared RBF MMD for every lead-feature distribution. It uses deterministic evenly spaced quantile-index sketches capped at 512 samples and a median-nonzero-distance bandwidth; estimator, bandwidth, sample counts, cap, and sampling rule are release-gated. The code change invalidates the otherwise valid pre-MMD artifact instead of retrofitting an unbound value.
15. Aggregate temporal rows could not support patient clustering or explicit reconstruction-side detector strata. Each evaluated model must now publish a compact full-denominator Parquet ledger with 26,376 record-lead-feature rows. The gate verifies its digest, identities, detector states, paired/unmatched accounting, and independently reconstructs aggregate counts, means, and variance ratios from sufficient statistics.
16. A single 100 ms association window could hide pairing-sensitive conclusions. The evaluator now computes a prespecified 25/50/75/100/150 ms sensitivity grid during the same detector pass. Its 60-row artifact is digest-bound; paired counts must be monotone nondecreasing, unmatched counts monotone nonincreasing, and the 100 ms slice must reproduce the primary aggregate.
17. The checkpoint catalog's historical `error` count was easy to mistake for a stalled live queue, and the original inference example could move disk growth from checkpoints to retained reconstruction outputs. The book and operator guide now separate live queue state, current compatibility, historical quarantine, logical remote bytes, local cache bytes, and output retention. A fresh archived-model run on the actual PTB-XL tensor strictly loaded `f_1000013_s42`, produced a finite `[1, 12, 5000]` result, and returned the checkpoint cache to zero. The unified 16-page book was then fully rerendered and passed normalized local-link and runtime-marker validation.
18. Chapter 12 named seeds 42/123/456 even though the executable manifest schedules 42/200/201. The chapter now parses and asserts all 480 unique identities, 160 masks per seed, the fixed MSE position, five binary positions, and five categorical kernel levels directly from the manifest.
19. The engineering narrative attributed ~100-scale composite losses mainly to derivative loss. Current compatible logs show ED-off models with median first validation total 1.7125 versus 107.6283 for ED-on models, despite similar median first validation MSE (0.1425 versus 0.1351). A mask-decoded, hash-bound artifact now exposes this scale diagnostic, and the prose states that total composite objectives are not comparable across masks.
20. The proposal used “seven-bit,” “seven main effects,” and architecture-dependent expanded-grid claims. It now states the implemented estimands: fixed MSE, five binary effects, categorical MMD-kernel contrasts, and prespecified interactions within one fixed MCMA architecture. The primary claim set was reduced from three overlapping claims to two focused claims.
21. Once the first four same-seed, same-binary-factor artifacts (`1000000`–`1000003`) were accepted, the engineering chapter began separating aggregate bias, variance-ratio closeness, distribution MMD, and detector coverage instead of manufacturing a composite winner. It joins the 26,376-row model ledgers on common finite record events and clusters repeated ECGs by patient. The resulting examples exposed the intended diagnostic: a kernel can improve paired-event MAE while worsening full-denominator detection coverage or aggregate bias. These remain one-seed, detector-conditioned operational contrasts, not confirmatory kernel effects.
22. The controlled seed-42 `100000*` family is now complete at all five kernel levels. A new builder-bound training contrast holds the five binary factors fixed and reports candidate-minus-level-0 validation MSE and runtime beside evaluator acceptance. All four kernel candidates have slightly higher final validation MSE than MSE-only, but their morphology directions differ: no candidate dominates absolute bias, variance-ratio closeness, distribution MMD, and detector coverage. Anatomical Laplacian gives the largest V6-QT common-pair MAE decrease (about 20.3 ms) while losing 6.64 percentage points of full-denominator coverage. This is direct evidence that optimization loss, morphology error, and measurement availability cannot be collapsed into one winner score; it remains descriptive one-seed evidence.
23. Direction counts alone still invited an informal rank, while averaging the adaptive-bandwidth distribution MMD across amplitudes and QT would be dimensionally misleading. A builder-bound Pareto audit now operates separately within each lead-feature row and requires all five kernels. Kernel 4 is nondominated on 11/12 rows, kernels 1 and 2 on 10/12, MSE-only on 6/12, and kernel 3 on 5/12. Pairwise dominance is reciprocal on some rows, making the absence of a universal winner explicit without endpoint weighting.
24. Evaluator lag was previously expressed only as accepted/compatible counts. The accepted artifact durations are now joined to the hash-bound training-duration summary: median service time is 17.80 minutes versus 21.59 minutes between model arrivals, or 3.37 versus 2.78 models/hour. Under uninterrupted stable medians the 13-model backlog drains at 0.59 models/hour and reaches the live frontier in about 21.9 hours. This is a conditional operational projection, not a scientific interval, and generation resets or contention invalidate it.
25. Checkpoint storage was re-audited under live load. The local SQLite file remains a transactional identity/provenance catalog rather than a blob container; exact float16 checkpoints live in a private immutable release and are materialized one at a time. A fresh real-data run strictly loaded `f_1000102_s42`, reconstructed `/home/mithunmanivannan/data/ptb_xl/tensors/test/100.pt` to a finite `[1, 12, 5000]` tensor, and pruned its dedicated cache to zero. The cohort audit independently covers all 20 current compatible models. Four byte-identical 311,496,307-byte comprehensive-result files were content-verified and filesystem-deduplicated, recovering approximately 0.87 GiB without deleting a path or changing its SHA-256.
26. A later live reconciliation kept two historical episodes distinct. The existing recovery ledger proves that the old 131 rows labeled completed contained 74 exit-zero runs and 57 partial/nonzero generations. Separately, a fresh census of the current log directory's 235 exit-code-1 markers found 218 missing-`ptbxl_database.csv` failures, 11 earlier tensor-inventory contract failures, and 6 runtime-finalization failures. None is a current-contract model completion. The repaired queue reached 33 completed models, all 33 passed strict finite-output inference, and 23 passed the final morphology evaluator. One additional run aborted at epoch 8 with `Invalid access of peer GPU memory over nvlink or a hardware error` (exit 134). The scheduler now distinguishes a narrow allowlist of transient GPU/driver faults from deterministic CUDA/program failures, retries them at most once after a delay, preserves their attempt count, and leaves all other non-OOM failures parked for inspection. The active training process survived the scheduler restart, proving resume continuity.
27. Growth from one to three complete same-seed kernel blocks exposed a book-only reshape assumption: the Pareto heatmap keyed rows only by kernel and endpoint, so distinct binary-prefix blocks collided. The render failed rather than silently aggregating duplicates. The heatmap now keys each row by seed, six-digit binary prefix, and kernel, asserts a complete nonmissing 5-by-12 matrix per block, and scales its height with block count. The surrounding prose also stopped freezing the first block's validation/runtime deltas and Pareto counts. A focused 28-cell chapter render and the full 16-page book render both passed afterward.

## Method-strength assessment

- **Problem fidelity:** strong; the frozen anchor is preserved.
- **Method specificity:** improved; generation, optimizer regime, data content, evaluation code, and per-model inference are explicit.
- **Contribution focus:** appropriately narrow; factorial endpoint discordance remains the dominant contribution.
- **Frontier leverage:** intentionally conservative and appropriate. No LLM, VLM, diffusion model, or new generator is required for the scientific question.
- **Feasibility:** training and storage are functioning, but the current median compatible-run duration implies a longer completion horizon than the old ETA.
- **Validation focus:** strong in design; incomplete in execution because the 480-model grid and generation-bound endpoint artifacts are unfinished.
- **Venue readiness:** not ready. Partial current-generation cells cannot support factorial effect estimates or clinical claims.

No numerical score is assigned because this review is not independent. In particular, it must not substitute for the requested Claude-family assessment.

## Remaining blocking risks

1. Complete all 480 current-contract model identities and verify archive/inference eligibility.
2. Ensure the cached-target temporal evaluator can keep pace with model production. The corrected target cache is complete and reused, but the explicit-distribution-MMD evaluator generation must repopulate model artifacts and intentionally accepts no earlier generation.
3. Repopulate the final evaluator generation. The code now supports tolerance sensitivity, detector-failure stratification, explicit distribution MMD, and patient-cluster summaries, but no earlier-generation artifact is admissible.
4. Complete all prespecified endpoint families and seed-aware factorial inference.
5. Justify non-inferiority margins before opening confirmatory external-test contrasts.
6. Obtain an independent reviewer when the requested bridge becomes available.

## Drift check

No conceptual drift is detected. The major changes remove unsupported evidence and make the original benchmark claim more testable; they do not replace it with a storage, visualization, or architecture paper.
