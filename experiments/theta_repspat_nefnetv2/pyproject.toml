[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "theta-repspat-nefnetv2"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy", "torch", "anndata"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
