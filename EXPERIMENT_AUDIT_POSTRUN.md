# Factorial v4 Postrun Audit Status

Overall verdict: **REVIEW_UNAVAILABLE**

The current snapshot passed its deterministic integrity checks: 62 focused
tests, core completeness, all 16 clinical completeness checks, and hash-bound
review of all 15 figures. A fresh read-only reviewer inspected the evaluation
source and artifacts, but the reviewer infrastructure did not emit a final
A–F verdict. The failed and partial attempts are preserved under
`.aris/traces/experiment-audit/`.

This is not an integrity PASS and must not be cited as one. The empirical
tables may be analyzed, but publication claims remain provisional until an
independent reviewer returns a final verdict.
